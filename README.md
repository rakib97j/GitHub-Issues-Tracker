1️⃣ What is the difference between var, let, and const?

Ans : var is function-scoped and can be re-declared and updated. let is block-scoped and can be updated but not re-declared. const is block-scoped and cannot be updated or re-declared.

2️⃣ What is the spread operator (...)?

Ans : The spread operator (...) is used to expand elements of an array or object. It helps to copy, merge, or pass values easily.

3️⃣ What is the difference between map(), filter(), and forEach()?

Ans : map() returns a new array after modifying each element. filter() returns a new array based on a condition. forEach() just loops through elements and does not return anything.

4️⃣ What is an arrow function?

Ans : An arrow function is a shorter way to write functions using =>. It makes the code cleaner and more concise.

5️⃣ What are template literals?

Ans : Template literals are strings written using backticks (`). They allow inserting variables using ${} and support multi-line strings.